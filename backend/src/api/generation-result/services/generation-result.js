'use strict';

/**
 * generation-result service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::generation-result.generation-result');
